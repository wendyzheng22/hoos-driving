import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hoos.driving',
  appName: 'hoos-driving',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
